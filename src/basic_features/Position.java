package src.basic_features;

public class Position
{
    char x_axis;
    int y_axis;

    public Position(char x_axis, int y_axis)
    {
        this.x_axis = x_axis;
        this.y_axis = y_axis;
    }
}
